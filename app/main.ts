import {bootstrap}    from 'angular2/platform/browser' //import {ComponentClassDefinitionName} from./path-to-file-name
import {AppComponent} from './app.component'

bootstrap(AppComponent); // here we say to angular that this is the entry point to our app that will load the rest of it for us
